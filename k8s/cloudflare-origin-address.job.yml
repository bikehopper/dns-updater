apiVersion: batch/v1
kind: CronJob
metadata:
  name: update-cloudflare-origin-address
spec:
  schedule: "* * * * *" # run every minute
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  concurrencyPolicy: Replace
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          containers:
            - name: update-cloudflare-origin-address
              image: ghcr.io/bikehopper/dns-updater@sha256:10cab034d687038b9cd46eea05c6ef64ebc55275a4eeebf530bf757f1ee51dfb
              resources:
                requests:
                  memory: "100Mi"
                  cpu: "50m"
              env:
                - name: ORIGIN_NAME
                  value: "us-west-1a"
                - name: DRY_RUN
                  value: "false"
                - name: CLOUDFLARE_BEARER_TOKEN
                  valueFrom:
                    secretKeyRef:
                      name: cloudflare-pool-edit-token
                      key: bearer
          imagePullSecrets:
            - name: dockerconfigjson-github-com
          restartPolicy: Never
