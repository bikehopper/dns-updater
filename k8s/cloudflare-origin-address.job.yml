apiVersion: batch/v1
kind: CronJob
metadata:
  name: update-cloudflare-origin-address
spec:
  schedule: "* * * * *" # run every minute
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  concurrencyPolicy: Replace
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          containers:
          - name: update-cloudflare-origin-address
            image: ghcr.io/bikehopper/dns-updater@sha256:c93eda321e35363408b682f736f0fc652fcd41d64ce276a09f53e073003effd9
            resources:
              requests:
                memory: "100Mi"
                cpu: "50m"
            env:
            - name: ORIGIN_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['topology.kubernetes.io/zone']
            - name: DRY_RUN
              value: 'true'
            - name: CLOUDFLARE_BEARER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: cloudflare-pool-edit-token
                  key: bearer
          imagePullSecrets:
          - name: dockerconfigjson-github-com
          restartPolicy: Never
